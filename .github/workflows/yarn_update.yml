name: Check for yarn updates

permissions:
  contents: write
  pull-requests: write

on:
  schedule:
    - cron: '0 5 1 * *'
  push:
    paths:
      - '.github/workflows/yarn_update.yml'
  workflow_dispatch:

jobs:
  yarn-update-check:
    runs-on: ubuntu-latest
    steps:
      - name: Update yarn
        uses: ffflorian/actions/yarn-update@1ef841db2bde72ed3537215f668b3f627569cfdd
        with:
          git_authorship: 'Florian Imdahl <git@ffflorian.de>'
