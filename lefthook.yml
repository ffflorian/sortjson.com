pre-commit:
  parallel: false
  commands:
    format:
      exclude:
        - CLAUDE.md
      glob: '*.{json,md,yml,js,ts,yaml}'
      run: npx --no oxfmt {staged_files} && git add {staged_files}
    oxlint:
      glob: '*.ts'
      run: npx --no oxlint -- --fix {staged_files} && git add {staged_files}
